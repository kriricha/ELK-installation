pipeline {
    agent any
    
    stages {
        stage('Build and Run Containers') {
            steps {
                script {
                    sh 'chmod +x startContainers.sh'
                    sh './startContainers.sh'
                }
            }
        }
    }
}
