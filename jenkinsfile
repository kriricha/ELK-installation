pipeline {
    agent any
    
    stages {
        stage('Build and Run Containers') {
            steps {
                script {
                    sh 'chmod +x startContainer.sh' // Ensure the script is executable
                    sh './startContainer.sh' // Execute the script
                }
            }
        }
    }
}
